# Static_power_on_shared

Altered default setup script <br /><br />
Added functionality:

    Power on unshared Static VM's prior to default setup
    Power on shared Static VM's if they are not in another reservation already
  
